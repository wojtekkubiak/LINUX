cat > “trust-policy.json” << “EOF”
{
    "Version": "2012-10-17",
    "Statement": [
        {
            "Effect": "Allow",
            "Princinpal": { "Service": "vmie.amazonaws.com"},
            "Action": "sts:AssumeRole",
            "Condition": {
                "StringEquals":{
                    "sts.Externalid": "vmimport"
                }
            }
        }
    ]
}

aws iam create-role --role-name vmimport --assume-role-policy-document "file://trust-policy.json"